# Unraid Templates for watchparty-v2

XML templates for Unraid Community Applications.

## Install
Copy `my-watchparty-v2.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
