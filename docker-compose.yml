services:
  watchparty-v2:
    image: muaeabudhabi/watchparty-v2:latest
    restart: unless-stopped
    labels:
      com.centurylinklabs.watchtower.enable: 'true'
      org.titan.app: watchparty-v2
    environment:
    - NODE_ENV=production
    - LOG_LEVEL=info
    - PUID=99
    - PGID=100
    - UMASK=022
    - DATABASE_URL=postgresql://watchparty-v2:changeme@watchparty-v2_postgres:5432/watchparty-v2
    - REDIS_URL=redis://watchparty-v2_redis:6379
    volumes:
    - /mnt/user/appdata/watchparty-v2/data:/data
    - /mnt/user/appdata/watchparty-v2/config:/config
    logging:
      driver: json-file
      options: &id001
        max-size: 10m
        max-file: '3'
    networks:
    - watchparty-v2_net
    depends_on:
      watchparty-v2_postgres:
        condition: service_healthy
      watchparty-v2_redis:
        condition: service_healthy
  watchparty-v2_postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
    - POSTGRES_DB=watchparty-v2
    - POSTGRES_USER=watchparty-v2
    - POSTGRES_PASSWORD=changeme
    volumes:
    - watchparty-v2_postgres_data:/var/lib/postgresql/data
    networks:
    - watchparty-v2_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-app}
      interval: 30s
      timeout: 5s
      retries: 5
  watchparty-v2_redis:
    image: redis:7-alpine
    restart: unless-stopped
    environment: []
    volumes:
    - watchparty-v2_redis_data:/data
    networks:
    - watchparty-v2_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 30s
      timeout: 5s
      retries: 5
networks:
  watchparty-v2_net:
    driver: bridge
volumes:
  watchparty-v2_data:
    driver: local
  watchparty-v2_postgres_data:
    driver: local
  watchparty-v2_redis_data:
    driver: local
